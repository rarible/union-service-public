spring:
  application:
    name: "union-api"

protocol:
  union:
    subscriber:
      broker-replica-set: ${kafka.hosts}

api:
  openapi:
    baseUrl: "https://${application.environment}-api.rarible.org"
  domains:
    ethereum: eth
