rarible:
  core:
    apm:
      agent:
        enabled: false
  loader:
    enableWorkers: true
    workers: 1
    enableRetrySchedulerJob: false

integration:
  tezos:
    client:
      url: "http://localhost:8080"

common:
  currency:
    refresh:
      cron: "-"
