rarible:
  core:
    apm:
      agent:
        enabled: false
  loader:
    enableWorkers: true
    workers: 1
    retry:
      backoffDelaysMillis: [ ]
      job:
        runner:
        period: 1000000000000000000 # Disable in the tests.

integration:
  tezos:
    client:
      url: "http://localhost:8080"

common:
  currency:
    refresh:
      cron: "-"
