protocol-union-api:
  image:
    repository: union-service-api
  replicas: 2
  environments:
    APPLICATION_ENVIRONMENT: '{{ include "service.appEnv" $ }}'
    CONSUL_ROOT_PATH: '{{ include "service.appEnv" $ }}/protocol/union'
    SPRING_PROFILES_ACTIVE: 'consul,{{ include "service.appEnv" $ }}'
    SERVICE_PREFIX: '{{ include "service.appEnv" $ }}-union'
    JAVA_OPTIONS: '-XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/tmp -agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=8000'
  nodeSelector:
    node.labels.rarible.service: true
  resources:
    jvm:
      xmx_megabytes: 384
      max_direct_memory_megabytes: 32
  updateConfig:
    order: start-first
  networks:
    common:
    - '{{ include "service.appEnv" $ }}-backend'
    - proxy-staging

protocol-union-listener:
  image:
    repository: union-service-listener
  environments:
    APPLICATION_ENVIRONMENT: '{{ include "service.appEnv" $ }}'
    CONSUL_ROOT_PATH: '{{ include "service.appEnv" $ }}/protocol/union'
    SPRING_PROFILES_ACTIVE: 'consul,{{ include "service.appEnv" $ }}'
    SERVICE_PREFIX: '{{ include "service.appEnv" $ }}-union'
  nodeSelector:
    node.labels.rarible.service: true
  resources:
    jvm:
      xmx_megabytes: 1536
      max_direct_memory_megabytes: 128
  networks:
    common:
    - '{{ include "service.appEnv" $ }}-backend'

protocol-union-meta-loader:
  image:
    repository: union-service-meta-loader
  environments:
    APPLICATION_ENVIRONMENT: '{{ include "service.appEnv" $ }}'
    CONSUL_ROOT_PATH: '{{ include "service.appEnv" $ }}/protocol/union'
    SPRING_PROFILES_ACTIVE: 'consul,{{ include "service.appEnv" $ }}'
    SERVICE_PREFIX: '{{ include "service.appEnv" $ }}-union'
  nodeSelector:
    node.labels.rarible.service: true
  resources:
    jvm:
      xmx_megabytes: 1024
      max_direct_memory_megabytes: 256
  networks:
    common:
      - '{{ include "service.appEnv" $ }}-backend'

protocol-union-indexer:
  enabled: false
  image:
    repository: union-service-indexer
  environments:
    APPLICATION_ENVIRONMENT: '{{ include "service.appEnv" $ }}'
    CONSUL_ROOT_PATH: '{{ include "service.appEnv" $ }}/protocol/union'
    SPRING_PROFILES_ACTIVE: 'consul,{{ include "service.appEnv" $ }}'
    SERVICE_PREFIX: '{{ include "service.appEnv" $ }}-union'
  nodeSelector:
    node.labels.rarible.service: true
  resources:
    jvm:
      xmx_megabytes: 1024
      max_direct_memory_megabytes: 512
  networks:
    common:
      - '{{ include "service.appEnv" $ }}-backend'

protocol-union-worker:
  enabled: false
  image:
    repository: union-service-worker
  environments:
    APPLICATION_ENVIRONMENT: '{{ include "service.appEnv" $ }}'
    CONSUL_ROOT_PATH: '{{ include "service.appEnv" $ }}/protocol/union'
    SPRING_PROFILES_ACTIVE: 'consul,{{ include "service.appEnv" $ }}'
    SERVICE_PREFIX: '{{ include "service.appEnv" $ }}-union'
  nodeSelector:
    node.labels.rarible.service: true
  resources:
    jvm:
      xmx_megabytes: 1024
      max_direct_memory_megabytes: 512
  networks:
    common:
      - '{{ include "service.appEnv" $ }}-backend'
